<form method="post" action="{{ path('app_audience_delete', {'id': audience.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ audience.id) }}">
    <button class="btn">Delete</button>
</form>
