<form method="post" action="{{ path('app_balance_delete', {'id': balance.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ balance.id) }}">
    <button class="btn">Delete</button>
</form>
