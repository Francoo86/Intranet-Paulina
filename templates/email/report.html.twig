<!DOCTYPE html>
<html lang="es">
  <head>
    <meta charset="UTF-8" />
    <meta
      name="viewport"
      content="width=device-width, initial-scale=1, shrink-to-fit=no"
    />
    <title>Tu Plantilla</title>
    <link
      href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css"
      rel="stylesheet"
    />
    <style>
      /* Agrega estilos personalizados si es necesario */
      .show-name {
        font-weight: bold;
        font-size: 1.2em;
      }
      .publicity-name {
        font-weight: bold;
        color: #007bff;
      }
      .publicity-data {
        margin-left: 20px;
      }
    </style>
  </head>
  <body>
    <div class="container mt-3">
      <div>
        <p>
          Número de Pauta: <span id="numeroPauta">{{ guidelineNumber }}</span>
        </p>
        <p>
          Fecha: <span id="fecha">{{ creationDate }}</span>
        </p>
        <p>
          Nombre Locutor:
          <span id="nombreLocutor"
            >{{ broadcasterFirstName }} {{ broadcasterLastName }}</span
          >
        </p>
      </div>

      {% for showData in showsData %}
      <div class="show-name">Show: {{ showData.name }}</div>

      {% for publicity in showData.publicities %}
      <div class="publicity-name">Publicidad: {{ publicity.sentence }}</div>

      <div class="publicity-data">
        <p>Duración: {{ publicity.duration }}</p>
        <p>Demografía: {{ publicity.demography }}</p>
        <p>Localidad: {{ publicity.locality }}</p>
        <p>Stock Amount: {{ publicity.stockAmount }}</p>
        <p>Balance Amount: {{ publicity.balanceAmount }}</p>
      </div>
      {% endfor %} {% endfor %}
    </div>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
  </body>
</html>
