{% extends 'base.html.twig' %}

{% block title %}Broadcaster{% endblock %}

{% block body %}
    <h1>Broadcaster</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ broadcaster.id }}</td>
            </tr>
            <tr>
                <th>First_name</th>
                <td>{{ broadcaster.firstName }}</td>
            </tr>
            <tr>
                <th>Last_name</th>
                <td>{{ broadcaster.lastName }}</td>
            </tr>
            <tr>
                <th>Email</th>
                <td>{{ broadcaster.email }}</td>
            </tr>
            <tr>
                <th>Phone</th>
                <td>{{ broadcaster.phone }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_broadcaster_index') }}">back to list</a>

    <a href="{{ path('app_broadcaster_edit', {'id': broadcaster.id}) }}">edit</a>

    {{ include('broadcaster/_delete_form.html.twig') }}
{% endblock %}
