{% block body %}
    <div class="row flex-lg-nowrap">
        <div class="col mb-3">
            <div class="e-panel card">
                <div class="card-body">
                    <div class="card-title">
                        <h6 class="mr-2">
                            <span>Pautas</span>
                        </h6>
                    </div>
                    <div class="e-table">
                        <div class="table-responsive table-lg mt-2">
                            <table class="table table-bordered">
                                <thead>
                                    <tr>
                                        <th class="sortable">ID</th>
                                        <th class="max-width">Nombre de locutor</th>
                                        <th class="max-width">Apellido de locutor</th>
                                        <th class="max-width">Correo</th>
                                        <th class="max-width">Telefono</th>
                                        <th class="max-width">Pautas que ejerce</th>
                                        <th>Acciones</th>
                                    </tr>
                                </thead>
                                <tbody id="output_table">
                                    {% include 'broadcaster/all_broadcaster.html.twig' %}
                                </tbody>
                            </table>
                        </div>
                    </div>
                </div>
            </div>
        </div>
        
        {% include 'search.html.twig' with {
            "newElement": "Nuevo locutor",
            'elementCount': broadcasters|length,
            "searchText": "Buscar pauta por nombre",
            "searchId": "busca_pautas",
            "searchPath": path('app_search_guideline'),
        } %}

        {% include 'modal_base_form.html.twig' with {
            'passedForm' : creationForm,
            'titleForm': 'Creación de pauta',
        }%}
    </div>
{% endblock %}