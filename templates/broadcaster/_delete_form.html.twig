<form method="post" action="{{ path('app_broadcaster_delete', {'id': broadcaster.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ broadcaster.id) }}">
    <button class="btn">Delete</button>
</form>
