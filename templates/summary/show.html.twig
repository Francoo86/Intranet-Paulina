{% extends 'base.html.twig' %}

{% block title %}Summary{% endblock %}

{% block body %}
    <h1>Summary</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ summary.id }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_summary_index') }}">back to list</a>

    <a href="{{ path('app_summary_edit', {'id': summary.id}) }}">edit</a>

    {{ include('summary/_delete_form.html.twig') }}
{% endblock %}
